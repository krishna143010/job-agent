[build]
builder = "NIXPACKS"
buildCommand = "./gradlew bootJar"

[deploy]
startCommand = "java -jar build/libs/job-agent.jar"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
